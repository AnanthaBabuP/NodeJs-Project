<!-- index.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <!-- Include Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        /* Set background image */
        body {
            background-image: url('https://images.unsplash.com/photo-1490730141103-6cac27aaab94?q=80&w=1920');
            background-position: center;
            background-size: cover;
            min-height: 100vh;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
        }

        /* Error message styling */
        .error_show {
            padding: 10px;
            align-self: center;
        }

        /* Container styling */
        .container {
            padding: 0px;
            margin: auto;
            margin-top: 10px;
            align-self: center;
        }

        /* Footer styling */
        .footer {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 10px 0;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
            box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1);
        }

        /* Header styling */
        .header {
            background-color: #f8f9fa;
            padding: 10px;
            display: flex;
            justify-content: space-between; /* Align items to the left and right */
            align-items: center;
        }

        /* Logo styling */
        .logo {
            width: 150px;
            height: auto;
        }

        .logo img {
            width: 100%;
            height: auto;
        }

        /* Login button styling */
        .login-button {
            margin-right: 10px; /* Adjust the margin as needed */
        }
    </style>
</head>
<body>
    <!-- Include the Header Component -->
    <header class="header">
        <div class="logo">
            <img src="../View/images/myLogo.jpg" alt="Your Logo">
        </div>
        <div class="login-button">
            <!-- Form for submitting to /login -->
            <form action="/login" method="get">
                <button type="submit" class="btn btn-primary">Login</button>
            </form>
        </div>
    </header>

    <div class="content container">
        <h1>Welcome to the Home Page</h1>
        <p>This is the content of the home page.</p>
    </div>

    <!-- Include the Footer Component -->
    <%- include('./footer.ejs') %>

    <script src="/scripts/main.js"></script> <!-- Link to your main JavaScript file -->
</body>
</html>
